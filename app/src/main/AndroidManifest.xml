<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.iowacityrobotics.rebuiltscoutingapp2026">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/main_theme">

        <activity
            android:name=".main_screens.StartScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".main_screens.SetupScreen" />

        <activity android:name=".pit_data.PitScouting" />

        <activity android:name=".match_data.DataEditor" />
        <activity android:name=".match_data.DataEntry" />

        <activity android:name=".main_screens.InstructionsScreen" />
        <activity android:name=".main_screens.Slider" />

    </application>

</manifest>